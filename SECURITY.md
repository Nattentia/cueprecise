# 보안 정책

## 지원 범위

최신 릴리스와 기본 브랜치의 최신 코드를 지원합니다. 보안 수정은 최신 릴리스에 먼저
적용되며 이전 시험판에는 소급 적용되지 않을 수 있습니다.

## API 키를 다루는 방식

CuePrecise는 사용자가 발급한 Gemini API 키로만 동작하며, 그 키는 사용자의 컴퓨터를 떠나지
않습니다.

키가 놓이는 곳과 그에 대해 하는 일은 다음과 같습니다.

- **Windows 설치 및 CLI 설정** — Windows DPAPI로 현재 사용자에게 묶어 암호화합니다.
  AI 앱 설정에는 키 대신 보호된 자격 증명 파일의 경로만 둡니다. 이전 버전의 평문 키는
  업그레이드할 때 자동 이관합니다.
- **Claude Desktop MCPB** — 민감 설정으로 선언하며 Claude의 확장 프로그램 설정 저장소가
  관리합니다.
- **macOS·Linux 앱 설정** — 현재는 MCP 클라이언트가 서버에 환경변수로 전달할 수 있도록
  해당 앱의 로컬 설정에 저장됩니다.
- **설정 백업** — 백업본에서 키를 지웁니다. 설정 파일 하나당 최근 3개까지만 남기므로
  폐기한 키가 사본에 남지 않습니다.
- **오류 메시지** — 화면에 나가기 전에 `***`로 가립니다. 값을 아는 자리에서는 값 자체를
  지우고, 값을 모르는 자리에서는 알려진 키 형식을 찾아 지웁니다.
- **명령줄** — `cueprecise setup`은 `--api-key-file`과 표준 입력(`--api-key -`)을 받습니다.
  키 값을 인자로 직접 주는 방식은 거절하므로 프로세스 목록과 셸 기록에 남지 않습니다.
- **사용량 원장** — 원문 대신 키의 SHA-256 해시만 기록합니다.

### 한계

같은 사용자 계정으로 실행되는 프로그램은 그 계정이 읽을 수 있는 모든 파일을 읽습니다.
macOS·Linux의 AI 앱 설정 파일과 Windows의 보호된 자격 증명 파일도 여기 포함됩니다.
위 조치는 실수로 인한 노출 — 화면 공유, 설정 파일을
게시물이나 이슈에 붙여넣기, 클라우드 동기화 폴더, 오래된 백업 — 의 범위를 줄이며, 이미
침해된 컴퓨터를 보호하지는 않습니다.

키가 노출됐다고 판단되면 [개인정보 정책의 폐기 절차](PRIVACY.md#api-키를-폐기하는-방법)를
따라 새 키를 발급하세요.

## 취약점 신고

보안 취약점, 비밀정보 노출, 임의 명령 실행 가능성은 공개 이슈로 등록하지 마세요. GitHub의 [비공개 취약점 신고](https://github.com/Nattentia/cueprecise/security/advisories/new)를 이용해 주세요.

신고에는 영향, 재현 절차, 영향을 받는 커밋 또는 환경, 가능한 완화책을 포함해 주세요. API 키, 쿠키, 비공개 영상이나 전사 원문은 필요한 최소 범위만 공유하세요.

가능하면 7일 안에 접수 여부를 답하고, 수정 일정과 공개 시점은 신고자와 조율하겠습니다.
