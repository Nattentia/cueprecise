# `summary.md` 구현 계획

상태: 설계 계획안. 이 문서는 구현 범위와 대안을 검토하며 `CONTRACT.md`와
충돌하면 `CONTRACT.md`를 따른다.

## 1. 목적과 역할

`derived/summary.md`는 영상을 다시 전사하거나 전체 전사를 호스트 LLM의
컨텍스트에 올리지 않고도 다음 세션에서 내용을 빠르게 복원하는 영속 문서다.

- `chapters.json`: 시간 경계와 탐색용 제목의 기준
- `index.sqlite3`: 질문에 답할 때 찾아가는 원문 근거
- `summary.md`: 사람이 읽는 전체 개요와 핵심 내용
- `merged.json`: 검증과 재생성에 쓰는 가장 상세한 텍스트

`summary.md`는 모든 발화를 보존하는 회의록도, 검색 인덱스의 복사본도 아니다.
요약에서 빠진 세부 사항은 `ytx_query`로 찾는다.

## 2. 권장 구조

```markdown
---
video_id: vRTcE19M-KE
transcript_fingerprint: ...
chapters_fingerprint: ...
generation: host-llm | local-extractive
---

# 영상 제목

## 한눈에 보기
2~4문장의 전체 개요

## 핵심 내용
- 주장 또는 설명 [00:03:12]

## 챕터별 정리
### 1. 챕터 제목 [00:00:00]
- 핵심 내용
- 필요한 경우에만 두 번째 내용

## 주요 용어
- **용어**: 영상 안에서 사용된 뜻 [00:12:30]
```

화자별 정리, 감상, 화면 설명, 긴 인용, 전체 자막 재수록은 기본 출력에서
제외한다. 정보 전달용 기술 영상의 요약과 이후 검색에 직접 필요한 내용만 둔다.

## 3. 생성 흐름

### 3.1 항상 가능한 로컬 폴백

사용자가 전체 요약을 요청해 `ytx_summary`를 호출할 때만 `chapters.json`의
제목·키워드·대표 문장으로 `summary.md`를 만든다. 기본 분석 파이프라인은 요약을
생성하지 않는다. 외부 모델 호출은 없고 Gemini 쿼터도 쓰지 않는다.

- 한눈에 보기: 영상 제목과 상위 키워드, 대표 문장을 짧게 조합
- 핵심 내용: 서로 다른 챕터의 대표 문장 최대 8개
- 챕터별 정리: 제목과 대표 문장 최대 2개
- 주요 용어: 여러 챕터에서 반복된 고유 용어만 수록

이 출력은 문장 품질이 다소 기계적이어도 완전하고 읽을 수 있어야 한다.
호스트가 후속 도구 호출을 지원하지 않는 환경의 최종 우회로이기도 하다.

### 3.2 선택적 호스트 LLM 작성

호스트 종류(Codex, Claude 등)에 의존하지 않도록 MCP sampling은 사용하지 않는다.
`ytx_summary`가 전체 전사 대신 압축된 작성 패킷을 반환한다.

- 영상 메타데이터
- transcript/chapters fingerprint
- 각 챕터의 ID, 시각, 확정 제목, 키워드, 대표 문장
- 결과 형식과 근거 제한

호스트는 패킷을 읽고 구조화된 요약을 한 번에 작성해
`ytx_set_summary`를 한 번 호출한다. 도구는 자유 형식 Markdown 전체를 받지 않고
다음 필드만 받는다.

- `overview`: 2~4문장
- `key_points`: `{text, chapter_ids}` 배열
- `chapter_summaries`: `{id, bullets}` 배열
- `terms`: `{term, meaning, chapter_ids}` 배열

도구가 챕터 ID를 실제 timestamp로 바꾸고 정해진 Markdown을 렌더한다.
따라서 호스트가 시간 경계나 파일 메타데이터를 임의로 바꿀 수 없다.

### 3.3 저장과 무효화

- atomic replace로 저장한다.
- 현재 transcript와 chapters fingerprint가 모두 일치할 때만 호스트 결과를 받는다.
- 같은 fingerprint로 재실행하면 `host-llm` 요약을 보존한다.
- 전사 또는 챕터가 바뀌면 stale로 보고 로컬 폴백을 재생성한다.
- `index`는 `summary.md`를 evidence로 색인하지 않는다. 요약 오류가 원문 근거처럼
  재인용되는 일을 막기 위해서다.

## 4. 토큰과 성능 예산

실측 Stanford 58분 영상의 챕터 제목용 대표 문장 패킷은 약 4.2KB였다.
요약도 같은 패킷을 재사용하고 챕터당 대표 문장을 최대 2개로 제한한다.

- 1시간 영상 목표 입력: 5~10KB
- 호스트 작업: 요약 작성 1회 + 저장 도구 1회
- 추가 Gemini 호출: 0회
- 매 대화에서 전체 전사 재주입: 없음
- 새 세션: `summary.md`부터 읽고, 세부 질문에만 SQLite 검색

챕터 제목 작성 직후라면 같은 패킷으로 요약까지 이어 중복 읽기를 줄일 수 있다.
다만 제목과 요약의 저장 도구는 분리해 한쪽 실패가 다른 쪽을 망치지 않게 한다.

## 5. 대안 검토

### A. Gemini에 별도 요약 요청

문장 품질은 일정할 수 있지만 무료 호출을 추가로 쓰고, 호스트가 이미 읽을 수 있는
패킷을 다시 외부 모델에 보낸다. 현재 목표에는 비용 대비 이점이 작아 제외한다.

### B. 호스트가 전체 전사를 직접 요약

구현은 단순하지만 긴 영상마다 입력 토큰과 지연이 크게 늘고 호스트 컨텍스트 한도에
따라 결과가 달라진다. 세부 근거 검색과 역할도 겹치므로 제외한다.

### C. MCP sampling으로 서버가 호스트 모델 호출

사용자 동작은 간단하지만 sampling 미지원 호스트가 있고 권한·프로토콜 차이로
이식성이 낮다. 핵심 경로로 쓰지 않는다.

### D. 로컬 추출 요약만 제공

가장 빠르고 모든 에이전트에서 동작하지만 주장 간 관계와 결론을 자연스럽게 압축하는
품질은 낮다. 단독 기본값이 아니라 필수 폴백으로 채택한다.

### E. 요약을 저장하지 않고 매 세션 즉석 생성

파일 로직은 줄지만 같은 토큰과 시간이 반복되고 결과도 흔들린다. 영속 컨텍스트라는
목적에 반하므로 제외한다.

### 결론

**요약 요청 시 로컬 추출본을 먼저 생성하고, 가능한 호스트에서는 압축 패킷으로 한 번
개선한 뒤 영속 저장하는 2단 구조**를 채택한다. 호스트 독립성, 실패 안전성, 토큰
절약과 문장 품질을 함께 만족하는 안이다.

## 6. 구현 순서

1. `src/summary.py`: fingerprint, 로컬 폴백, 호스트 결과 검증, Markdown 렌더
2. 파이프라인: 변경하지 않음. 요약은 필수 단계가 아니라 온디맨드 캐시
3. MCP: `ytx_summary`와 `ytx_set_summary` 추가
4. 테스트
   - 호스트 없이 생성
   - 모든 챕터가 순서대로 한 번씩 등장
   - timestamp는 `chapters.json`에서만 렌더
   - stale fingerprint, 없는/중복 chapter ID, 빈 본문 거부
   - rebuild 시 host 요약 보존, 입력 변경 시 폴백 재생성
5. 실제 영상 검증
   - 58분 Stanford 번들의 생성 시간, 파일 크기, 패킷 크기 측정
   - 한국어 영상에서 복원된 영어 용어가 요약에 유지되는지 확인

## 7. 완료 조건과 중단선

- 네트워크와 LLM 없이 유효한 `summary.md`가 생성된다.
- 호스트가 달라도 같은 구조화 입력과 저장 도구를 쓴다.
- 호스트가 잘못된 timestamp나 chapter ID를 저장할 수 없다.
- 추가 Gemini 호출이 없다.
- 1시간 영상의 호스트 패킷이 목표 10KB 이하다.
- 세부 질문은 요약이 아니라 원문 index 근거로 답한다.

여기까지 충족하면 1차 구현을 끝낸다. 재귀 요약, 벡터 DB, 별도 요약 모델,
화자별 논지 추적, 자동 사실 검증은 실제 실패 데이터가 생기기 전에는 추가하지 않는다.
