# CuePrecise에 기여하기

버그 보고와 기능 제안, 문서 및 코드 기여를 환영합니다. 큰 인터페이스 변경은 구현 전에 이슈에서 방향을 합의해 주세요.

## 개발 환경

Python 3.11 이상이 필요합니다. 저장소를 복제한 뒤 다음을 실행합니다.

```bash
python -m venv .venv
python -m pip install -r requirements.txt
python -m unittest discover -s tests
```

테스트는 네트워크나 Gemini API를 호출하지 않습니다. 실제 영상으로 검증할 때는 API 키, 쿠키, 원본 영상, 자막 및 전사 결과를 커밋하지 마세요.

## 변경 원칙

- `CONTRACT.md`는 단계 사이 데이터와 공개 실행 경로의 기준입니다. CLI, MCP 도구, JSON 구조, 저장 경로를 바꾸면 계약과 테스트를 함께 갱신해 주세요.
- 버그 수정에는 가능하면 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 추가해 주세요.
- 파생 산출물을 조용히 삭제하거나 기존 번들을 호환되지 않게 바꾸지 마세요.
- 새 런타임 의존성은 필요한 이유와 선택 가능한 대안을 PR에 적어 주세요.
- 사용자에게 보이는 오류와 문서는 현재 프로젝트의 한국어 문체를 유지해 주세요.

## Pull request

작고 독립적으로 검토할 수 있는 변경을 권장합니다. PR 설명에 문제, 해결 방식, 실행한 검증, 호환성 영향을 적어 주세요. CI에서 지원 Python 버전의 전체 테스트가 통과해야 합니다.

보안 문제는 공개 이슈를 만들지 말고 `SECURITY.md`의 절차를 따라 주세요.

